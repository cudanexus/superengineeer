const fs = require('fs'); const os = require('os'); const path = require('path'); const settingsPath = path.join(os.homedir(), '.superengineer', 'settings.json'); const fallbackPath = path.join(process.cwd(), 'data', 'settings.json'); const file = fs.existsSync(settingsPath) ? settingsPath : fallbackPath; console.log(file, fs.existsSync(file) ? JSON.parse(fs.readFileSync(file, 'utf8')).appendSystemPrompt : 'not found');
